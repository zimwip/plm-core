{
  "permissions": {
    "allow": [
      "Bash(docker compose logs *)",
      "Bash(docker compose ps *)",
      "Bash(docker compose config *)",
      "Bash(podman ps *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); fp=d.get('file_path',''); name=fp.split('/')[-1]; blocked=name=='.env' or name.startswith('.env.'); print('Blocked: .env files are off-limits — edit manually') if blocked else None; sys.exit(2) if blocked else sys.exit(0)\""
          }
        ]
      }
    ]
  }
}
